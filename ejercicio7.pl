elsif ($contador_archivos == 2) {
		open salida, ">$fichero_salida";
		foreach $linea(@fichero_entrada) {
			$contador_lineas++;
			if (($contador_lineas + $partes) <= $partes) { 
				print salida $linea;
			}
			else { 
				print "escrito fichero $fichero_salida con $contador_lineas lineas\n";
				close salida;
				last; 
			}
		}
	}
	elsif ($contador_archivos == 3) {
		open salida, ">$fichero_salida";
		foreach $linea(@fichero_entrada) {
			$contador_lineas++;
			if (($contador_lineas + $partes) <= $partes) { 
				print salida $linea;
			}
			else { 
				print "escrito fichero $fichero_salida con $contador_lineas lineas\n";
				close salida;
				last; 
			}
		}
	}
	elsif ($contador_archivos == 4) {
		open salida, ">$fichero_salida";
		foreach $linea(@fichero_entrada) {
			$contador_lineas++;
			if (($contador_lineas + $partes) <= $partes) { 
				print salida $linea;
			}
			else { 
				print "escrito fichero $fichero_salida con $contador_lineas lineas\n";
				close salida;
				last; 
			}
		}
	}
	elsif ($contador_archivos == 5) {
		open salida, ">$fichero_salida";
		foreach $linea(@fichero_entrada) {
			$contador_lineas++;
			if (($contador_lineas + $partes) <= $partes) { 
				print salida $linea;
			}
			else { 
				print "escrito fichero $fichero_salida con $contador_lineas lineas\n";
				close salida;
				last; 
			}
		}
	}
	elsif ($contador_archivos == 6) {
		open salida, ">$fichero_salida";
		foreach $linea(@fichero_entrada) {
			$contador_lineas++;
			if (($contador_lineas + $partes) <= $partes) { 
				print salida $linea;
			}
			else { 
				print "escrito fichero $fichero_salida con $contador_lineas lineas\n";
				close salida;
				last; 
			}
		}
	}
	elsif ($contador_archivos == 7) {
		open salida, ">$fichero_salida";
		foreach $linea(@fichero_entrada) {
			$contador_lineas++;
			if (($contador_lineas + $partes) <= $partes) { 
				print salida $linea;
			}
			else { 
				print "escrito fichero $fichero_salida con $contador_lineas lineas\n";
				close salida;
				last; 
			}
		}
	}
	elsif ($contador_archivos == 8) {
		open salida, ">$fichero_salida";
		foreach $linea(@fichero_entrada) {
			$contador_lineas++;
			if (($contador_lineas + $partes) <= $partes) { 
				print salida $linea;
			}
			else { 
				print "escrito fichero $fichero_salida con $contador_lineas lineas\n";
				close salida;
				last; 
			}
		}
	}
	elsif ($contador_archivos == 9) {
		open salida, ">$fichero_salida";
		foreach $linea(@fichero_entrada) {
			$contador_lineas++;
			if (($contador_lineas + $partes) <= $partes) { 
				print salida $linea;
			}
			else { 
				print "escrito fichero $fichero_salida con $contador_lineas lineas\n";
				close salida;
				last; 
			}
		}
	}
	elsif ($contador_archivos == 10) {
		open salida, ">$fichero_salida";
		foreach $linea(@fichero_entrada) {
			$contador_lineas++;
			if (($contador_lineas + $partes) <= $partes) { 
				print salida $linea;
			}
			else { 
				print "escrito fichero $fichero_salida con $contador_lineas lineas\n";
				close salida;
				last; 
			}
		}
	}
	elsif ($contador_archivos == 11) {
		open salida, ">$fichero_salida";
		foreach $linea(@fichero_entrada) {
			$contador_lineas++;
			if (($contador_lineas + $partes) <= $partes) { 
				print salida $linea;
			}
			else { 
				print "escrito fichero $fichero_salida con $contador_lineas lineas\n";
				close salida;
				last; 
			}
		}
	}
	elsif ($contador_archivos == 12) {
		open salida, ">$fichero_salida";
		foreach $linea(@fichero_entrada) {
			$contador_lineas++;
			if (($contador_lineas + $partes) <= $partes) { 
				print salida $linea;
			}
			else { 
				print "escrito fichero $fichero_salida con $contador_lineas lineas\n";
				close salida;
				last; 
			}
		}
	}
	elsif ($contador_archivos == 13) {
		open salida, ">$fichero_salida";
		foreach $linea(@fichero_entrada) {
			$contador_lineas++;
			if (($contador_lineas + $partes) <= $partes) { 
				print salida $linea;
			}
			else { 
				print "escrito fichero $fichero_salida con $contador_lineas lineas\n";
				close salida;
				last; 
			}
		}
	}
	elsif ($contador_archivos == 14) {
		open salida, ">$fichero_salida";
		foreach $linea(@fichero_entrada) {
			$contador_lineas++;
			if (($contador_lineas + $partes) <= $partes) { 
				print salida $linea;
			}
			else { 
				print "escrito fichero $fichero_salida con $contador_lineas lineas\n";
				close salida;
				last; 
			}
		}
	}
	elsif ($contador_archivos == 15) {
		open salida, ">$fichero_salida";
		foreach $linea(@fichero_entrada) {
			$contador_lineas++;
			open salida, ">$fichero_salida";
			if (($contador_lineas + $partes) <= $partes) { 
				print salida $linea;
			}
			else { 
				print "escrito fichero $fichero_salida con $contador_lineas lineas\n";
				close salida;
				last; 
			}
		}
	}
	elsif ($contador_archivos == 16) {
		open salida, ">$fichero_salida";
		foreach $linea(@fichero_entrada) {
			$contador_lineas++;
			if (($contador_lineas + $partes) <= $partes) { 
				print salida $linea;
			}
			else { 
				print "escrito fichero $fichero_salida con $contador_lineas lineas\n";
				close salida;
				last; 
			}
		}
	}
	elsif ($contador_archivos == 17) {
		open salida, ">$fichero_salida";
		foreach $linea(@fichero_entrada) {
			$contador_lineas++;
			if (($contador_lineas + $partes) <= $partes) { 
				print salida $linea;
			}
			else { 
				print "escrito fichero $fichero_salida con $contador_lineas lineas\n";
				close salida;
				last; 
			}
		}
	}
	else { last; }